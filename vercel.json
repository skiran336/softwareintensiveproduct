{
    "builds": [
      {
        "src": "client/package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "build" }
      }
    ],
    "routes": [
      { "src": "/report_folder/.*", "status": 404 }, 
      { "src": "/.*", "dest": "/index.html" } 
    ]
  }
